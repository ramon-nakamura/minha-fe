## Packages
framer-motion | For beautiful floating bubble animations and page transitions
date-fns | For formatting message dates

## Notes
Replit Auth is already configured - use the provided `useAuth` hook and direct users to `/api/login` when they need to authenticate.
The backend handles the endpoints `/api/messages` (GET, POST), `/api/messages/:id/like`, and `/api/messages/:id/pardon`.
